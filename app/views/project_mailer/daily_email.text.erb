<%= card_count_header(@project.current) %> in progress:

<%= render :partial => 'card', :collection => @project.current %>

<%= card_count_header(@project.backlog) %> in the backlog prioritized as follows:

<%= render :partial => 'card', :collection => @project.backlog %>
